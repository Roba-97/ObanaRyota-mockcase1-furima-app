const modal = document.getElementById("js-chat-modal");
const button = document.getElementById("js-modal-open-button");

button.addEventListener('click', () => {
    modal.style.display = "block";
});